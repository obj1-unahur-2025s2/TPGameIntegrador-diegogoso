import wollok.game.*
import clases.*
import hechizos.*

program gameGeneral {
  game.title("gameGeneral")
  game.height(15)
  game.width(15)

 
  game.onCollideDo({fuego, algo => algo.recibirAtaque(fuego)})
  game.play()
}